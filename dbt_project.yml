name: "ff"
version: "1.0.0"
config-version: 2

profile: "nfl"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ff:
    public:
      +materialized: table
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    fact:
      +materialized: table

